# ============ P0: 正确性问题 ============

# FIXME(xihong): MinerU 空结果被静默当成解析成功
# 位置: mineru_client.py:86 — results 为空时直接 return {"md_content": "", ...}
# 场景: 扫描版PDF(未开OCR)、MinerU返回200但业务报错等情况下,
#       解析实际失败却被标记成功,任务一路跑到complete,
#       内容为空、0表格/分块/向量,抽取结果全空。
# 影响: 用户看到"处理完成"但无任何数据,且无 *_failed 记录可排查。
# 方案: 空内容时应抛异常或显式置为 parsing_failed。

# FIXME(xihong): build_page_mapping 的 LIS 清洗不检测跳变,孤立假锚污染页码
# 位置: utils/page_mapping.py:144-146 — _longest_nondecreasing_keep 只保证
#       page_num 非降,孤立的大跳变假唯一锚会被保留。
# 现象: 生产库 24 条 chunk 声称 page_num=146-437(跨292页),实际字符跨度仅 378 字。
# 影响: 污染 chunk/表格页码、bbox 高亮、前端定位与模型自报页码(约 0.1%)。
#       extraction 侧已在 split_span_by_pages 做局部防御(_MIN_CHARS_PER_PAGE),
#       但根因未除,其余下游仍受影响。
# 方案: LIS 之后增加跳变剔除;存量文件走 POST /file/{id}/recompute_page_mapping 重算。


# ============ P0: 吞吐问题 ============

# FIXME(xihong): 同步调用阻塞整个 asyncio 事件循环(两处,吞吐杀手)
# 位置1: search_service.py:44 — async协程里跑同步gRPC(Milvus检索),
#        且每次请求重建连接 + load()
# 位置2: vl_service/model.py:49, locate.py:63/147 — fitz光栅化 + PIL编码
#        (CPU密集持GIL)直接跑在事件循环里
# 影响: 单次调用期间所有并发请求(含前端高频轮询)全部冻结,
#       global_max_concurrency=8 形同虚设。
# 方案: 用 asyncio.to_thread 把阻塞调用移出事件循环;
#       Milvus 连接应复用而非每次重建。


# ============ P1: 成本可观测性缺失 ============

# FIXME(xihong): 文本/表格 LLM 与 embedding 的 usage token 被丢弃
# 影响: 无成本统计,而 LLM 调用是核心成本项,无法追踪/预警/分摊。
# 方案: 在 LLM/embedding 调用返回处采集 usage,落库或至少打日志聚合。


# ============ P2: 前端体验 ============

# FIXME(xihong): 前端批量上传无并发控制 + 每文件各刷一次列表、各弹一个Toast
# 影响: 批量上传时列表频繁重渲染、Toast刷屏,体验差,
#       且无并发限制可能打满后端请求。
# 方案: 上传加并发队列(如3~5并发); 批量完成后统一刷新一次列表、
#       合并为一个汇总Toast。


# ============ P2: 死代码 ============

# FIXME(xihong): 启动依赖自检模块 startup_check 从未被调用
# 位置: startup_check 模块
# 影响: 文档宣称的探活表根本不打印,实际未生效,存在文档与代码脱节。
# 方案: 要么在启动流程里接入调用,要么删除该模块并更新文档。

# 表名识别限制为 10 路，但这是每个文档单独的限制，不是整个服务全局限制：service/table_service.py:204。5 个文档可能放大成 50 路模型请求。  这不合理  应该是用于总的限制

# 前项目里的 MinerU 并发配置没有实际使用。5 个文件同时进入 parsing，本项目会同时向 MinerU 发出 5 个请求， 应当加一个排队中的状态， 这里的的MinerU队列宽度的限制需要实际使用上，然后，还需要加上一个pipeline的配置 设置pipe的队列宽度

#当前项目虽然会把上传的 PDF 保存到 uploads 目录，但上传接口仍使用 await file.read() 将整个 PDF 一次性加载到内存，并把完整 bytes 传给后台 pipeline；调用 MinerU 时也直接用这份内存数据发送。与此同时，mineru.queue_width 配置目前没有被代码使用，
  因此多个用户同时上传时，系统会无限制地并发调用 MinerU，并在最长 1200 秒的请求等待期间持续持有各个 PDF 的完整内存数据。例如 10 个 500MB 文件同时解析，仅原始 PDF 就可能占用约 5GB 内存，还未计算 Markdown、图片渲染、Base64 和进程本身的开销，容
  易导致内存暴涨、任务超时或服务崩溃。建议将上传改为分块流式写入 uploads/{file_id}.pdf.part，上传完成并校验成功后原子重命名为正式 PDF；后台 pipeline 只传 file_id 或文件路径，MinerU multipart 请求使用文件流发送，避免再次 read_bytes()；同时让
  queue_width 真正通过全局 Semaphore 控制 MinerU 并发，初始建议设为 2。实现时需要处理上传中断产生的临时文件、数据库写入失败后的文件补偿清理、运行中文件不能被保留策略或删除接口提前删除、应用重启后的失败恢复，以及 VL 阶段仍可能整份读取 PDF 和
  生成 Base64 的独立内存问题。单任务可能因磁盘读取增加很小的延迟，但操作系统文件缓存通常会减弱影响；在多用户并发场景下，整体稳定性和实际吞吐通常会明显改善。