在调试界面发现 如果来源为表格  应该加一个字段 表格匹配词  实际检索表格时应该根据多个词检索 前端中的表名匹配模式 应该改成 表名 然后用户提示词需要引入搜索结果  点击右上角X图标加入  当前加入的是表名  但是当前在字符串填充时代码写的好像需要使用实际的表名才能被填充 这显然不合理  应该就是使用前端的表名 自动使用前面搜索匹配表格匹配到的表
当前表格匹配的方式应该加一个AI匹配  它能够看到当前文件的所有表格名 然后选择最相似的表格, 来源为文本时使用章节检索也需要加一个ai匹配的方式 当使用ai匹配时显示表格、章节选择提示词，在右侧的调试流里面也需要输出打印
需要新增使用vl端到端的抽取方式 有两种 一种是取起始页数给vl  一种是全文并发 每个单独收集简短信息（只输出有效信息，无效不汇总） 最终汇总给文本模型  一种是两步定位，几页一起缩略图给模型定位，然后把定位到的页面给Vl 一种是从首页开始使用历史对话累计提取（对话历史需要处理，不要图片，输出需要简短有效） 这些各个阶段的提示词都需要配置  支持调试
解析失败需要支持重试

需要支持多类型文件：也就是可以配置多套抽取字段和逻辑规则  