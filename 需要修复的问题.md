5. MinerU 空结果被静默当成解析成功 — mineru_client.py:86(results 为空即 return {"md_content":"",...})
  扫描版 PDF(未开 OCR)、MinerU 200 返业务错误等,解析实际失败却被标记成功,文件一路跑到 complete,内容为空、0
  表格/分块/向量、抽取全空,用户看到"处理完成"却无任何数据,也无 *_failed 可排查。→ 空内容时抛异常或置 parsing_failed。

6. 同步调用阻塞整个 asyncio 事件循环(两处,吞吐杀手)
  - Milvus 检索:search_service.py:44 在 async 协程里跑同步 gRPC,且每次请求重建连接 + load()
  - VL 渲染:vl_service/model.py:49、locate.py:63/147 的 fitz 光栅化 + PIL 编码(CPU 密集持 GIL)直接跑在事件循环里
  单次调用期间所有并发请求(含前端高频轮询)全部冻结,global_max_concurrency=8 形同虚设。→ 用 asyncio.to_thread
  把阻塞调用移出事件循环。

优化点: 前端批量上传无并发控制 + 每个文件各刷一次列表、各弹一个 Toast

优化点: 文本/表格 LLM 与 embedding 的 usage token 被丢弃 → 无成本统计(LLM 是核心成本)

优化点: 启动依赖自检模块 startup_check 从未被调用(死代码),文档宣称的探活表根本不打印